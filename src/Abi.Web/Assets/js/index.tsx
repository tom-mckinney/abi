import * as React from "react";
import * as ReactDOM from "react-dom";

import ContentTagger from "./components/ContentTagger";

ReactDOM.render(
  <ContentTagger />,
  document.getElementById("content-tagger"),
);
